{
  "description": "Filter by fee amount",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "default": "Amount"
    },
    "amount": {
      "type": "integer"
    },
    "condition": {
      "type": "string",
      "enum": [
        "LESS_THAN_EQUAL",
        "LESS_THAN",
        "GREATER_THAN",
        "GREATER_THAN_EQUAL'"
      ]
    }
  },
  "additionalProperties": false,
  "required": ["type", "amount", "condition"]
}
