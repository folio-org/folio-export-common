type: object
properties:
  operation:
    type: string
    enum:
      - AND
      - OR
  criteria:
    type: array
    items:
      $ref: bursarExportFilter.yaml
    minItems: 1
required:
  - operation
  - criteria
