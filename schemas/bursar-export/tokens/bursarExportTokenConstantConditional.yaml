type: object
properties:
  conditions:
    type: array
    items:
      type: object
      properties:
        condition:
          $ref: ../filter/bursarExportFilter.yaml
        value:
          $ref: bursarExportTokenConstant.yaml
      required:
        - condition
        - value
  else:
    $ref: bursarExportTokenConstant.yaml
additionalProperties: false
required:
  - conditions
  - else
