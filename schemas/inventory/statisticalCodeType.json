{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "A statistical code type",
  "StatisticalCodeType": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "unique ID of the statistical code type; a UUID"
      },
      "name": {
        "type": "string",
        "description": "name or description of a statistical code type"
      },
      "source": {
        "type": "string",
        "description": "label indicating where the statistical code type originates from, i.e. 'folio' or 'local'"
      },
      "metadata": {
        "type": "object",
        "$ref": "../common/metadata.json#/Metadata"
      }
    },
    "required": [
      "name",
      "source"
    ]
  }
}